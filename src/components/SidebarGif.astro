---
const gifs = [
  { src: '/images/sidebar/1.gif', alt: '1' },
  { src: '/images/sidebar/2.gif', alt: '2' },
  { src: '/images/sidebar/3.gif', alt: '3' },
{ src: '/images/sidebar/4.gif', alt: '4' },
{ src: '/images/sidebar/5.gif', alt: '5' },
]
---

<aside class="gif-sidebar">
  {gifs.map((g) => (
    <img class="gif-item" src={g.src} alt={g.alt} loading="lazy" />
  ))}
</aside>

<style>
  .gif-sidebar {
    position: fixed;
    top: 120px;
    right: 0px;
    width: 150px;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    z-index: 20;
    pointer-events: none; /* không chặn click vào website */
  }

  .gif-item {
    width: 100%;
    border-radius: 8px;
    border: 1px solid rgba(255,255,255,0.1);
    pointer-events: auto;
  }

  @media (max-width: 1024px) {
    .gif-sidebar {
      display: none;
    }
  }
</style>
